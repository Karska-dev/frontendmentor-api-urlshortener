import React from 'react';

const Shortener = () => (
  <div className="Shortener">
  <input type="url" value="Shorten a link here..." required/>
  <input type="button" value="Shorten It!"/>
  </div>
)

export default Shortener;